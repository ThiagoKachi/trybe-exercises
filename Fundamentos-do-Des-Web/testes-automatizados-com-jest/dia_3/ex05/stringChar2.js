const toUpper = (str) => { return str.toUpperCase()}
const firstChar = (str) => { return str[0]}
const concatStrings = (str1, str2) => { return str1 + str2}

module.exports = {
    toUpper,
    firstChar,
    concatStrings,
}